<li class="all"><a href="#">全部</a></li>
{% if categories_list.first[0] == null %}
  {% for category in categories_list %} 

    <li class="{{ category | join: "_" }}"><a href="{{ BASE_PATH }}{{ site.JB.categories_path }}#{{ category| join: "_" }}">{{ category | join: "/" }}-<span>{{ site.categories[category].size }}</span></a></li>
  {% endfor %}
{% else %}
  {% for category in categories_list %} 

    <li class="{{ category[0] | join: "_" }}"><a href="{{ BASE_PATH }}{{ site.JB.categories_path }}#{{ category[0]| join: "_" }}">{{ category[0] | join: "/" }}-<span>{{ category[1].size }}</span></a></li>

  {% endfor %}
{% endif %}

{% assign categories_list = nil %}